# Admin Compare Center — browser waiver (partial)

**Production bundle check:** `unified-ledger-tieout` route string **found** in `assets/index-DmXmyuH_.js` on `erp.dincouture.pk`.

**Live UI load:** NOT VERIFIED — no authenticated admin session in ops browser check.

**Expected when logged in as admin/developer:**

- `/admin/unified-ledger-tieout` loads all tabs (Party, Pilot Batch, Account, Trial Balance, Cash/Bank)
- Party tab MR JALIL golden regression
- Pilot Batch 9/9 historical PASS

**Action:** Ops opens Compare Center after login; screenshot Party tab + Pilot Batch summary.
