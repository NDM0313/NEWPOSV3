# RESIDUAL_RISKS.md

1. **Salesman / limited / branch-restricted live RLS** — passwords not in approved local env (`NOT_RUN_CREDENTIAL_GATED`). Active salesman emails exist (e.g. `noman@yahoo.com` / DIN BRIDAL).
2. **Authenticated APK emulator matrix** incomplete this closure run — emulator process instability / adb hang after reinstall (`EMULATOR_QA_FAIL`).
3. **Physical device QA** not run (`NOT_RUN_DEVICE_GATED`).
4. Contact-list vs period statement closing — expected basis difference; must stay labelled.
5. Aging operational vs official GL — expected basis difference.
6. Some edit/void/return/rental invalidation may still rely on realtime/UI refresh.
7. Dirty `main` WIP must not be included in any merge.
