# APK install — MOBILE APK DEVICE QA

**Generated:** 2026-07-01  
**Status:** BLOCKED — install not attempted

No adb device connected. `adb install` was **not** run.

When a device is available:

```bash
adb install -r erp-mobile-app/releases/internal-qa/20260701/dincouture-erp-internal-qa-20260701-debug.apk
```

Expected package: `com.dincouture.erp` (versionName 1.0.5 / versionCode 39).
