# BUILD_RESULTS.md

Not executed in this Phase 1 session.

Suggested commands when authorized:

```bash
cd erp-mobile-app
npm run build:mobile:prod
npm run cap:sync:android:prod
cd android && ./gradlew assembleDebug
```

Record APK path, Gradle exit code, and install log here when run.
