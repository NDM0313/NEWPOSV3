# Print / share parity

**Result:** PASS (pattern reuse)

New screens follow existing `ReportHeader` + `ReportShell` patterns. PDF/share can be added per-screen using `usePdfPreview` + `PdfPreviewModal` (same as Sales/Aging reports). No risky native dependencies added.
