# RESIDUAL_RISKS.md

1. Live salesman/branch-restricted **server RLS** not exercised (`NOT_RUN_CREDENTIAL_GATED`).
2. Emulator authenticated report matrix incomplete (WebView automation).
3. Physical device QA not run.
4. Contact-list vs statement period closing remains an expected basis difference — must stay labelled.
5. Some edit/void/return/rental invalidation still rely on realtime/UI refresh (documented in finalization).
6. Dirty `main` WIP still exists separately — do not merge over it blindly.
