# Commit Review — `c5f64247` ("AR deve")

**Date:** 2026-06-12  
**Safe production baseline:** `483255e7` — *Classify Saqib rental payment as AR/AP metadata review*  
**Reviewed commit:** `c5f64247` — *AR deve*  
**Parent:** `483255e7` (one commit ahead of VPS)

---

## 1. Summary

| Metric | Value |
|--------|-------|
| Files changed | **228** |
| Insertions / deletions | +40,284 / −6,384 |
| Commit message quality | Poor — truncated, does not describe scope |
| Scope | Mixed — AR/ledger/roznamcha, print UI, Flutter Phase 8B, mobile sync, docs, SQL apply scripts, graphify |

This commit is **not deployable as a unit**. It bundles unrelated feature work with GL-adjacent service changes and manual SQL apply scripts.

---

## 2. Files changed (by category)

### 2.1 AR / AP / reconciliation / ledger alignment

| File | Change |
|------|--------|
| `scripts/sql/deploy_verify_ar_ap_phase21_readonly.sql` | Added — read-only deploy verify |
| `scripts/sql/deploy_verify_ar_ap_readonly.sql` | Added — read-only deploy verify |
| `scripts/sql/deploy_verify_order_sales_readonly.sql` | Added — read-only deploy verify |
| `scripts/sql/diag_ar_ap_center_baseline_audit.sql` | Modified — diagnostic |
| `docs/accounting/LEDGER_V2_VS_ACCOUNT_STATEMENTS_DATA_SOURCES.md` | Added — ledger V2 data-source doc |
| `docs/LEDGER_V2_GL_ALIGNMENT_REPORT.md` | Added |
| `docs/LEDGER_STATEMENT_CENTER_V2_IMPLEMENTATION_REPORT.md` | Added |
| `docs/LEDGER_STATEMENT_CENTER_V2_TECH_NOTE.md` | Added |
| `src/app/features/ledger-statement-center-v2/LedgerStatementCenterV2Page.tsx` | Modified |
| `src/app/services/ledgerDataAdapters.ts` | Modified |
| `src/app/services/customerLedgerTypes.ts` | Modified |
| `erp-mobile-app/src/api/partyGlLedger.ts` | Modified |
| `erp-mobile-app/src/components/accounts/reports/PartyLedgerReport.tsx` | Modified |
| `erp-mobile-app/src/components/accounts/reports/AccountLedgerReport.tsx` | Modified |

**Note:** No AR/AP Phase 3 apply UI, migrations, or RPC apply paths were added. Changes are docs, diagnostics, and ledger display adapters — but mixed with unrelated work.

### 2.2 GL / payment / journal logic (high sensitivity)

| File | Risk |
|------|------|
| `src/app/services/roznamchaService.ts` | **High** — +339 lines, branch/expense rules |
| `src/app/services/commissionPaymentService.ts` | **High** — new commission payment flow |
| `src/app/services/paymentLifecycleService.ts` | **High** — payment lifecycle extension |
| `src/app/services/addEntryV2Service.ts` | **Medium** — general entry path |
| `src/app/components/accounting/AddEntryV2.tsx` | **Medium** |
| `src/app/components/shared/UnifiedPaymentDialog.tsx` | **High** |
| `src/app/components/accounting/CommissionPaymentDialog.tsx` | **High** — new |
| `src/app/components/accounting/TransactionMutationConfirmDialog.tsx` | **Medium** — new |
| `src/app/hooks/useTransactionMutationConfirm.tsx` | **Medium** — new |
| `src/app/lib/resolveDefaultPaymentAccount.ts` | **Medium** — new |
| `src/app/lib/roznamchaBranchMatch.ts` | **Medium** — new |
| `src/app/lib/roznamchaExpenseRules.ts` / `roznamchaExpense.test.ts` | **Medium** |
| `src/app/lib/salePaymentHistoryFilter.ts` | **Medium** |
| `src/app/utils/journalEntryDescription.ts` | **Low–Medium** |
| `erp-mobile-app/src/api/openingBalanceJournal.ts` | **High** — new |
| `erp-mobile-app/src/api/roznamcha.ts` | **High** |
| `erp-flutter-app/lib/data/repositories/journal_write_repository.dart` | **High** — new |
| `erp-flutter-app/lib/features/ledger/screens/journal_create_screen.dart` | **High** — new |
| `erp-flutter-app/lib/data/repositories/studio_finalize_after_invoice.dart` | **High** — new |

### 2.3 Manual SQL apply / fix scripts (do not run on production without review)

| File | Type |
|------|------|
| `scripts/sql/apply_ren_0002_rental_paid_reconcile.sql` | **Mutating apply** |
| `scripts/sql/apply_roznamcha_ge_branch_je0017_jv000202.sql` | **Mutating apply** |
| `scripts/sql/apply_roznamcha_missing_general_entry_payments.sql` | **Mutating apply** |
| `scripts/sql/apply_roznamcha_null_branch_general_entry.sql` | **Mutating apply** |
| `scripts/sql/fix_expense_payment_type_for_roznamcha.sql` | **Mutating fix** |
| `scripts/sql/preview_roznamcha_*.sql` | Preview only |
| `scripts/sql/diag_*.sql` (roznamcha, ren_0002, company branch) | Read-only diag |
| `scripts/sql/verify_roznamcha_ge_je0017_jv000202.sql` | Verify |

### 2.4 Migrations

| File | Risk |
|------|------|
| `migrations/20260610120000_companies_currency_symbol.sql` | **Low** — additive display columns on `companies` only |

**No GL, payment, journal, sales, or AR/AP schema migrations.**

### 2.5 Deploy orchestration (`deploy/`)

**Not touched.** No changes to Docker, nginx, Kong, or deploy shell scripts.

Read-only deploy **verify** SQL was added under `scripts/sql/deploy_verify_*` — not deploy pipeline code.

### 2.6 Flutter (erp-flutter-app) — Phase 8B offline / print / counter

38 files: Drift offline DB, counter worker PIN, Sunmi/ErpPrinter channel, journal create, studio finalize-after-invoice, POS lock, build scripts, `graphify-out/manifest.json` (+17k lines).

### 2.7 Mobile Capacitor (erp-mobile-app) — print / reports / roznamcha

~90 files: ledger statement PDF, staff statement, fiscal year, currency formatting, printing settings, roznamcha API, opening balance journal, date range presets, share messages.

### 2.8 Web UI — print / reports / formatting (non-GL)

Ledger column layout editor, A4 report preview frame, printing preview panels, barcode print, classic print base, currency/debit-credit In/Out labels, styles.

### 2.9 Roznamcha / expense (web)

`roznamchaService.ts`, `ExpensesDashboard.tsx`, `AddExpenseDrawer.tsx`, `ExpenseContext.tsx`, `docs/accounting/ROZNAMCHA_HOME_EXPENSE_FIX.md`.

### 2.10 Graphify / tooling / misc

| Item | Notes |
|------|-------|
| `graphify-out/GRAPH_REPORT.md` | Large regen |
| `graphify-out/.graphify_root` | Modified |
| `erp-flutter-app/graphify-out/manifest.json` | New (+17k lines) |
| `scripts/capture-*-screenshots.mjs` | Screenshot tooling |
| `docs/screenshots/**` | Print fixture PNGs/PDFs |
| `GIT_WORKFLOW_RULES.txt` | **Accidental** — example commit message changed to `"AR deve"` |
| `package.json` / `package-lock.json` | Minor deps |

### 2.11 Unrelated to stated "AR deve" scope

- Flutter offline DB + thermal printing + counter PIN
- Mobile staff statement + ledger PDF export
- Commission payment dialog + SOP doc
- Currency symbol migration + formatCurrency across clients
- Print pagination screenshots and ledger column layout editor
- Graphify manifest bulk adds

---

## 3. Checklist (requested)

| Question | Answer |
|----------|--------|
| Files changed | 228 |
| AR/AP related | ~14 files (docs, diag SQL, ledger adapters — no Phase 3 apply) |
| Unrelated | ~180+ files (Flutter, mobile print, graphify, screenshots, commission, currency UI) |
| Migrations included | **Yes — 1** (`companies_currency_symbol`, display-only) |
| Deploy scripts touched | **No** (`deploy/` unchanged); read-only verify SQL added |
| GL/payment/journal logic touched | **Yes — extensively** (web roznamcha, commission, payments; mobile opening balance; Flutter journal write) |
| Flutter/mobile/graphify/ledger/roznamcha | **All included** in same commit |

---

## 4. Risk level

**HIGH**

Reasons:

1. Single commit mixes money-path services with print/mobile/Flutter/docs.
2. Four `apply_*` and one `fix_*` SQL scripts can mutate GL/payments if run manually.
3. Commit message and `GIT_WORKFLOW_RULES.txt` edit indicate accidental squash, not reviewed release.
4. Production VPS remains at safe `483255e7`; deploying this would skip controlled review per slice.
5. Does **not** implement AR/AP Phase 3, but touches adjacent reconciliation and ledger surfaces.

Individual slices (currency migration, print UI, Flutter offline) may be **Low–Medium** in isolation; **as committed, High**.

---

## 5. Recommendation

**Backup + revert from `main`; split later.**

| Action | Rationale |
|--------|-----------|
| `git branch backup/c5f64247-ar-deve c5f64247` | Preserve all work |
| `git push origin backup/c5f64247-ar-deve` | Remote backup |
| `git revert c5f64247` on `main` | Restore tree to `483255e7` content without force-push |
| Do **not** deploy `c5f64247` | Per safe stable mode |
| Do **not** run `migrations/20260610120000_*` on VPS until reviewed | Even low-risk migration waits for deploy approval |
| Split backup into topical branches later | e.g. `feature/flutter-phase8b`, `feature/print-ledger-v2`, `fix/roznamcha-branch`, `feature/currency-symbol` |

**Not recommended:** keep on `main`, reset `--hard`, or deploy as-is.

---

## 6. Decision log

| Field | Value |
|-------|-------|
| Decision | **Backup + revert** |
| Backup branch | `backup/c5f64247-ar-deve` |
| VPS deploy | Unchanged — stays `483255e7` |
| AR/AP Phase 3 | Blocked |
| Financial Trace Phase 1 | Continue read-only (`FINANCIAL_TRACE_PHASE1_*` docs/SQL only) |

---

## 7. References

- Safe baseline: `483255e727f26e653949ecf303a21167cdea0aa7`
- Reverted commit: `c5f64247add3e2b8b52c0bbc2e45f587c9a86ae4`
- Phase 1 plan: `docs/accounting/2026-06-12_FINANCIAL_TRACE_PHASE1_DIAGNOSTIC_PLAN.md`
