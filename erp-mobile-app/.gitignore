# Stale compiled Vite config shadows vite.config.ts and breaks dev proxy (404 on /auth)
vite.config.js

node_modules
dist
.env
.env.local
.env.production
.env.*.local

# Local release APK copies for sideload / GitHub uploads (large binary; do not commit)
releases/*.apk
