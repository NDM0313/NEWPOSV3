# Copy to .env for local dev (gitignored).
# Or pass via flutter run:
#   flutter run --dart-define=SUPABASE_ANON_KEY=your_key_here

# Anon key: copy VITE_SUPABASE_ANON_KEY from repo root .env.production
SUPABASE_ANON_KEY=

# Locked native API base (do not change for production Android)
SUPABASE_URL=https://erp.dincouture.pk
