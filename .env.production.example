# Copy to .env.production and fill (optional – on VPS, deploy-erp-vps.sh uses /root/supabase/docker/.env for ANON_KEY)
# Used when running: docker compose -f docker-compose.prod.yml build
VITE_SUPABASE_URL=https://72.62.254.176:8443
VITE_SUPABASE_ANON_KEY=your_anon_key_from_supabase_docker_env
