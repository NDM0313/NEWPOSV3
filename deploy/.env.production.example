# Production build env - used at DOCKER BUILD time (Vite inlines VITE_* at build)
# Copy to .env.production and fill values. Then build with:
#   docker compose -f deploy/docker-compose.prod.yml --env-file .env.production up -d --build
# If these are missing at build, the app will throw on load and auth will hit the wrong host (401).

# Supabase (self-hosted on VPS) - URL = full URL the BROWSER must call (HTTPS)
# Kong API typically port 8000; put behind Nginx/Caddy with SSL.
VITE_SUPABASE_URL=https://your-supabase-api-url
VITE_SUPABASE_ANON_KEY=your-anon-key

# Set to true to disable Realtime WebSocket (avoids 403 when Kong/Traefik block WS)
VITE_DISABLE_REALTIME=true
