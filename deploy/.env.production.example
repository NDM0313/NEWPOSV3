# Production build env - used at docker build time
# Copy to .env.production and fill values before: docker compose -f deploy/docker-compose.prod.yml build

# Supabase (self-hosted on VPS) - URL = what the BROWSER calls (HTTPS)
# Kong API typically port 8000; put behind Nginx/Caddy with SSL.
VITE_SUPABASE_URL=https://your-supabase-api-url
VITE_SUPABASE_ANON_KEY=your-anon-key
